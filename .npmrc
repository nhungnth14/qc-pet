# Expo + React Native cần node_modules phẳng (Metro bundler giả định hoisting).
# node-linker=hoisted làm node_modules giống npm, để các transitive deps
# (vd @react-navigation/native do expo-router kéo về) import được trực tiếp.
node-linker=hoisted
